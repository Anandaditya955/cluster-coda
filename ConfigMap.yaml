apiVersion: v1
kind: ConfigMap

metadata:
  name: aditya-configure
  namespace: prod-ns

data:
  DB_HOST: "mongodb"
  DB_PORT: "27017"
  DB_USER: "admin"
  APP_NAME: "studentapp"
  APP_PORT: "5978"
  config.txt: |
    This is ConfigMap volume file for studentapp.